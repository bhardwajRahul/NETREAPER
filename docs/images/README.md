# Screenshots

Add terminal screenshots here:

- `main-menu.png` - NETREAPER main menu
- `scan-output.png` - Example scan output
- `status.png` - Status command output

To capture:
1. Run `netreaper` in terminal
2. Screenshot with: `gnome-screenshot -a` or `scrot -s`
3. Save here as PNG
